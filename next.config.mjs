/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['images.unsplash.com','img.freepik.com','www.freepik.com'], // 👈 allow Unsplash images
  },
};

export default nextConfig;
